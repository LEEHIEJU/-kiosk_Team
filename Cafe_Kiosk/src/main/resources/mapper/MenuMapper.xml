<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hizju.cafekiosk.dao.MenuDAO">
	<!-- namespace를 인터페이스 이름과 일치 시켜야 한다. -->
	
	<!-- 메뉴리스트 -->
	<select id="menulist" resultType="MenuVO">
		select
			*
		from 
			MENU
	</select>
	
	<!-- 메뉴 상세정보 -->
	<select id="menuinfo" parameterType="string" resultType="MenuVO">
		select
			*
		from 
			MENU
		where foodnum = #{foodnum}
			 
	</select>
	
	<!-- 메뉴 저장 -->
	<insert id="insert" parameterType="MenuVO">
	insert into MENU (foodnum, foodname, foodtype, foodprice, foodsoldout, foodlev)
		values (#{foodnum},
		 		#{foodname}, 
                #{foodtype},
                #{foodprice}, 
                #{foodsoldout},
                #{foodlev})
	</insert>
	
	<!-- 메뉴 수정 -->
	<update id="update" parameterType="MenuVO">
	update MENU set foodname=#{foodname}, foodtype=#{foodtype}, foodprice=#{foodprice}, 
					foodsoldout=#{foodsoldout}, foodlev=#{foodlev}
			where foodnum=#{foodnum}
	</update>
	
	<!-- 메뉴 삭제 -->
	<delete id="delete" parameterType="string">
	delete from MENU where foodnum= #{foodnum, jdbcType=VARCHAR}
	</delete>
	
	
	</mapper>
