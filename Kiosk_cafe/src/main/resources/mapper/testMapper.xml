<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hizju.cafekiosk.dao.MenuDAO">
	<!-- namespace를 인터페이스 이름과 일치 시켜야 한다. -->
	
	<select id="menu" parameterType="Menu" resultType="kr.hizju.cafekiosk.vo.MenuVO">
		select 
		FOODNUM AS foodnum,
		FOODNAME AS foodname,
		FOODTYPE AS foodtype,
		FOODPRICE AS foodprice,
		FOODSOLDOUT AS foodsoldout,
		FOODLEV AS foodlev
		from MENU
		
	</select>
	
</mapper>